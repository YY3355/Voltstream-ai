__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
venv/
env/
.env
.vscode/
.idea/
*.csv
# the DART paper-book ledger IS the git-audited track record — must be tracked
!journal/*.csv
# deterministic test fixtures are tracked so checkers/CI need no generate step
!tests/fixtures/*.csv
*.db
*.sqlite3
*.log
# structured job-run log for the daily-rhythm agents — local runtime state, not the audit trail
journal/jobs.jsonl
.DS_Store
.claude/settings.local.json
models/*.pkl
models/*.h5
models/*.pt
dart_cache/
# ERCOT catalog + archiver outputs (re-fetchable; may hold large raw JSON)
data_archive/
